{
  "name": "ng-utils",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ng/utils/src",
  "prefix": "dasch-ng",
  "tags": [],
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/ng/utils/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/ng/utils/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/ng/utils/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/ng/utils/jest.config.ts"
      },
      "configurations": {
        "update": {
          "updateSnapshot": true
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "publish": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public",
        "distFolderPath": "dist/libs/ng/utils"
      },
      "dependsOn": ["build"]
    },
    "github": {
      "executor": "@jscutlery/semver:github"
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "postTargets": ["ng-utils:publish", "ng-utils:github"]
      }
    }
  }
}
