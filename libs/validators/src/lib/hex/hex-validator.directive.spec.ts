import { HexValidatorDirective } from './hex-validator.directive';

describe('HexValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HexValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
