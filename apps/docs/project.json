{
  "name": "docs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/docs",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitepress dev apps/docs"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitepress build apps/docs"
      },
      "outputs": ["{projectRoot}/.vitepress/dist"]
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitepress preview apps/docs"
      }
    },
    "docs:generate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["typedoc", "cp apps/docs/api-readme-template.md apps/docs/api/README.md", "vitepress build apps/docs"],
        "parallel": false
      },
      "outputs": ["{projectRoot}/.vitepress/dist", "{projectRoot}/api"],
      "dependsOn": ["^build"]
    }
  },
  "tags": ["type:app", "scope:docs"],
  "implicitDependencies": []
}
